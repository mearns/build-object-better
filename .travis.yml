language: node_js

node_js: ["latest", "stable", "13", "12", "10", "8"]

cache: npm

script:
  - npm run build

jobs:
  include:
    - stage: publish docs 📜
      script: npm run docs
      deploy:
        edge: true # opt in to dpl v2
        provider: pages:git
        token: $GITHUB_TOKEN
        local_dir: out/docs
        cleanup: false
        verbose: true
        on:
          branch: master
